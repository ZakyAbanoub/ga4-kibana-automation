# Reference data — client file, must NOT be public
reference-data.xlsx
*.xlsx
*.csv

# Secrets: service-account keys, env files
secrets/
.env
.env.*
!.env.example

# Ignore stray JSON (e.g. downloaded SA keys) but keep project config files
*.json
!package.json
!package-lock.json
!tsconfig.json
!vercel.json

# Build / deps / local output
node_modules/
.vercel/
dist/
.out/
.vercel

# Local copy of the Vercel CRON_SECRET
.cron-secret.txt

# Local copy of the Vercel PAGES_API_TOKEN
.pages-api-token.txt

# Handoff secret bundle (out-of-band, never committed)
handoff/
